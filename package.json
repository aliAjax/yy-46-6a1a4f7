{
  "name": "doc-flow-static",
  "version": "1.0.0",
  "private": true,
  "description": "Static government document workflow demo with lightweight local tooling.",
  "scripts": {
    "dev": "node scripts/serve.js",
    "test": "node --test tests/*.test.js",
    "lint": "node --check js/data.js && node --check js/app.js && node scripts/check-html-css.js"
  },
  "engines": {
    "node": ">=18"
  }
}
